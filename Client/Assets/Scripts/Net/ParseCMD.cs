using System;
using AsyncSocket;
using UnityEngine;

public class ParseCMD
{
    public static void Parse(DataBase data)
    {
        switch (data.cmd)
        {
            default:
                break;
        }
    }
}
